spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/nomad-coder/msa
          search-paths:
            - configs
#         search-paths: '*,{application},config/{application}, config/, config/*'
server:
  port: 8888